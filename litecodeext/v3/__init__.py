"""LiteCode v3 core — pluggable provider layer + workflow engine.

Everything here is opt-in. Legacy `litecodeext/` code is untouched.
Import surface: `from litecodeext.v3 import llm_registry, vision_registry, workflow_engine`.
"""
__version__ = "0.1.0-dev"
